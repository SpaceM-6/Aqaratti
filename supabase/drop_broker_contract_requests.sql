-- ============================================================================
-- إزالة ميزة "طلب التعاقد" بالكامل (زر وجدول) بطلب صريح - الميزة لم تُستخدم
-- فعلياً من أي زائر حقيقي، وقُرِّر استبدالها بعرض بيانات تواصل الوسيط مباشرة
-- بدل حجبها خلف طلب. الجدول أُنشئ في broker_contract_requests.sql (يبقى
-- كسجل تاريخي لما تم تطبيقه سابقاً)، وهذا الملف يُسقطه فعلياً.
-- ============================================================================

drop table if exists public.broker_contract_requests;
